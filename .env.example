# =============================================================================
# .env.example — Template de variáveis de ambiente para HeimdallWeb
# =============================================================================
#
# COMO USAR:
#   cp .env.example .env
#   # Edite .env com seus valores reais
#   docker compose up -d
#
# SEGURANCA:
#   O arquivo .env NUNCA deve ser comitado no repositório.
#   Ele já está listado no .gitignore.
#   Para produção, use um secret manager (HashiCorp Vault, AWS Secrets Manager, etc.)
# =============================================================================


# ─── PostgreSQL ───────────────────────────────────────────────────────────────
# Credenciais do banco de dados PostgreSQL 16.
# Estes valores são usados tanto pelo container postgres quanto pela connection
# string montada pelo docker-compose para o serviço api.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=db_heimdall


# ─── .NET WebApi ──────────────────────────────────────────────────────────────
# Ambiente de execução do ASP.NET Core.
# Valores válidos: Development | Staging | Production

ASPNETCORE_ENVIRONMENT=Development


# ─── JWT Authentication ───────────────────────────────────────────────────────
# Chave secreta usada para assinar tokens JWT.
# REQUISITO: Mínimo de 32 caracteres. Use um gerador de strings aleatórias.
# Exemplo de geração: openssl rand -base64 48

JWT_KEY=your_jwt_secret_key_minimum_32_characters_here

# Issuer e Audience devem corresponder aos valores esperados pelos clientes JWT.
JWT_ISSUER=HeimdallWeb
JWT_AUDIENCE=HeimdallWebUsers


# ─── Google Gemini AI ─────────────────────────────────────────────────────────
# Chave de API para o serviço Google Gemini (análise de vulnerabilidades).
# Obtenha em: https://makersuite.google.com/app/apikey

GEMINI_API_KEY=your_gemini_api_key_here


# ─── Next.js Frontend ────────────────────────────────────────────────────────
# URL da API backend acessível pelo BROWSER (client-side).
# Em desenvolvimento local: http://localhost:5110
# Em produção: URL pública da API (ex: https://api.heimdallweb.com)
#
# NOTA: Variáveis prefixadas com NEXT_PUBLIC_ são expostas ao browser.
#       Nunca coloque segredos com este prefixo.

NEXT_PUBLIC_API_URL=http://localhost:5110
